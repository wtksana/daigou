<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daigou.core.dao.GoodsMapper">
    <!-- Result Map-->
    <resultMap id="baseResultMap" type="com.daigou.core.domain.Goods">
        <result column="uuid" property="uuid"/>
        <result column="type" property="type"/>
        <result column="name" property="name"/>
        <result column="price" property="price"/>
        <result column="bid" property="bid"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- dg_goods table all fields -->
    <sql id="base_column_list">
        uuid,type,name,price,bid,create_time,update_time,status,remark
    </sql>

    <!-- 查询条件 -->
    <sql id="base_where_clause">
        where 1=1
        <trim suffixOverrides=",">
            <if test="uuid != null and uuid != '' and uuid != ''">
                and uuid = #{uuid}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
            <if test="name != null and name != '' and name != ''">
                and name = #{name}
            </if>
            <if test="price != null and price != ''">
                and price = #{price}
            </if>
            <if test="bid != null and bid != ''">
                and bid = #{bid}
            </if>
            <if test="createTime != null and createTime != ''">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null and updateTime != ''">
                and update_time = #{updateTime}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="remark != null and remark != '' and remark != ''">
                and remark = #{remark}
            </if>
        </trim>
    </sql>

    <!-- 插入记录 -->
    <insert id="save" parameterType="com.daigou.core.domain.Goods">
        insert into dg_goods (
        <trim suffixOverrides=",">
            uuid ,
            <if test="type != null  and type != '' ">
                type,
            </if>
            <if test="name != null  and name != '' ">
                name,
            </if>
            <if test="price != null  ">
                price,
            </if>
            <if test="bid != null  ">
                bid,
            </if>
            <if test="status != null  ">
                status,
            </if>
            <if test="remark != null  and remark != '' ">
                remark,
            </if>
            create_time,
        </trim>
        ) values (
        <trim suffixOverrides=",">
            #{uuid} ,
            <if test="type != null  and type != '' ">
                #{type},
            </if>
            <if test="name != null  and name != '' ">
                #{name},
            </if>
            <if test="price != null  ">
                #{price},
            </if>
            <if test="bid != null  ">
                #{bid},
            </if>
            <if test="status != null  ">
                #{status},
            </if>
            <if test="remark != null  and remark != '' ">
                #{remark},
            </if>
            now(),
        </trim>
        )
    </insert>

    <!-- 根据id，修改记录-->
    <update id="update" parameterType="com.daigou.core.domain.Goods">
        update dg_goods set type=#{type},name=#{name},price=#{price},bid=#{bid},update_time=now(),status=#{status},remark=#{remark} where uuid=#{uuid}
    </update>

    <!-- 删除记录 -->
    <delete id="deleteByUuid" parameterType="com.daigou.core.domain.Goods">
        update dg_goods set status=0 where uuid = #{uuid}
    </delete>

    <!-- 根据id查询 商品表 -->
    <select id="getByUuid" resultMap="baseResultMap" parameterType="com.daigou.core.domain.Goods">
        select
        <include refid="base_column_list"/>
        from dg_goods where uuid = #{uuid}
    </select>

    <!-- 商品表 列表总数-->
    <select id="countByModel" resultType="java.lang.Integer" parameterType="com.daigou.core.domain.Goods">
        select count(1) from dg_goods
        <include refid="base_where_clause"/>
    </select>

    <!-- 根据pages查询 -->
    <select id="getListByPages" resultMap="baseResultMap" parameterType="com.daigou.core.util.Pages">
        select
        <include refid="base_column_list"/>
        from dg_goods where 1=1
        <if test="option != null and option != ''">
            and (name like CONCAT('%','${option}','%' ) or type=#{option} )
        </if>
    </select>

    <select id="getGoodsTypes" resultType="String">
        select * from dg_goods_type
    </select>

</mapper>
